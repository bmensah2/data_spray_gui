{
  "name": "aben-spray-report-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Generates session/spray-event .docx reports (generate_report_rgb.js). Run `npm install` to fetch node_modules — it is gitignored, not committed.",
  "main": "generate_report_rgb.js",
  "scripts": {
    "report": "node generate_report_rgb.js"
  },
  "dependencies": {
    "docx": "^9.7.1"
  }
}
